package engine;

//import org.junit.jupiter.api.Test;

import java.awt.*;

class DrawLoadingTest {
    /*
        int width = screen.getWidth()/2 , height = width/2;
		Graphics2D g2 = (Graphics2D) backBufferGraphics;

		g2.setColor(new Color(0,255,0, 230));
		g2.fillRect(x,y, width, height);
    drawLoadingString(x + width/5 ,y+(width*18)/44,"LOADING");


    int out_x = x + width + screen.getWidth()/30, out_width = screen.getWidth()/10;
		g2.setColor(new Color(0,255,0, 222));
		g2.fillRect(out_x,y, out_width, height);

    int dx = screen.getWidth()/65;
		g2.setColor(Color.black);
		g2.fillRect(out_x + dx, y + dx, out_width - 2*dx, height - 2*dx);


    int startX = out_x + dx + dx/2, startY = y + dx +dx/2 ,
            endX = startX + out_width -2*dx -dx, endY = startY + height - 2*dx -dx;
    loadingProgress(startX, startY, endX, endY ,g2);


		g2.setColor(Color.black);
		g2.fillRect(x + (width*3)/44 , y+(width*3)/44, (width/44)*38,(height / 44)*22);
    animateLoading(x + (width*3)/44,y+(width*3)/44);


		g2.setStroke(new BasicStroke(2));
		g2.setColor(Color.white);
		g2.drawRect(x-1,y-1, width+2, height+2);
		g2.setColor(new Color(255,255,255, 222));
		g2.drawRect(out_x-1,y-1, out_width+2, height+2);

    timercount++;

    @Test
    void drawEntity() {
    }

    @Test
    void drawLoadingString() {
        //정확한 위치에 draw 하고 있는지, 제대로 깜박이고 있는지 test

        if ( y is under the animate box && y is over the loading box || x is left-bottom side of animate box
                                    && x is inner in loading box){

        }
        if (testcount is  0 over 160 below)

    }

    @Test
    void loadingProgress() {
        //제대로된 위치인지, parameter 확인
        //black box가 제대로 늘어나고 있는지, countdown동안 100%가 다 그려지는지
        if (endOfTimercount >= height){

        }
    }

    @Test
    void drawLoading() {

    }

    @Test
    void animateLoading() {
        // 박스에 비해 크기는 적당한지.
        //간격이 적당하니
        if( x1 + 15 this.SpriteType.values()[5].width > x2 )
        if( x1 + 60 this.SpriteType.values()[6].width > x3 )
        if( x1 + 100 this.SpriteType.values()[8].width > x4 )
        if( x1 + 145 this.SpriteType.values()[10].width > x5 ){
        }
        //박스 안에서 움직이는지
        if (x1 < animatebox.startX && animatebox.endX - width > x1 + 145){
        }
        if (y1 > animatebox.y && height < animatebox.height){
        }
        //monster가 적당한 크기 인지 448 , 33 , 73/1000 -> 0.075
        if (ratio < screen.width){
        }
    }

     */
}